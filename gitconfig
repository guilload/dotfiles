[alias]
    ad = add -p
    br = branch
    cm = commit -v -S
    cma = commit --amend -v -S
    co = checkout
    cob = checkout -b
    cp = cherry-pick -S
    cpa = cherry-pick --abort
    cpc = cherry-pick --continue
    df = diff
    fclean = clean -fdx
    lg = log --graph --oneline --decorate --all
    ph = push
    phf = push --force-with-lease
    phfo = push --force-with-lease origin
    pho = push origin
    pl = pull
    plo = pull origin
    plom = pull origin master
    rb = rebase
    rbm = rebase master
    rbc = rebase --continue
    rbi = rebase -i -S
    rh = reset --hard
    rma = git ls-files --deleted | git rm
    rs = reset
    rsh = reset HEAD
    rshard = reset --hard
    sp = stash pop
    ss = stash save
    st = status
[color]
    ui = auto
[commit]
    gpgSign = true
[core]
    editor = vim
    excludesFile = ~/.gitignore
    sparseCheckout = true
    precomposeUnicode = true
[gpg]
    program = gpg
[merge]
    conflictStyle = diff3
[pull]
    rebase = true
[push]
    default = simple
[rebase]
    autoSquash = true
    autoStash = true
[rerere]
    enabled = true
[user]
    name = Adrien Guillo
    email = adrien.guillo@gmail.com
    signingkey = D752F5C83B224044
[includeIf "gitdir:~/Workspace/repositories/airbnb/**"]
    path = ~/Workspace/repositories/airbnb/.gitconfig
